# Milestone 1 needs no secrets, accounts, or environment variables to run.
# VITE_* values are public browser configuration. Never put tokens here.
# Add the PUBLIC Spotify client ID in milestone 2, after creating the app.
# VITE_SPOTIFY_CLIENT_ID=
